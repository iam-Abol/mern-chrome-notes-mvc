<%- include('../includes/header.ejs') %>
    <%- include('../includes/navbar.ejs') %>


        <ul class="m-auto w-4/5 p-2">
            <% notes.forEach(note=> {%>

                <div
                    class="rounded-2xl bg-white p-4 mb-4 shadow-md hover:shadow-xl shadow-stone-300 transition-all duration-300">
                    <div class="mb-2 flex items-center justify-between">
                        <h2 class="text-xl font-semibold text-gray-950">
                            <%= note.title %>
                        </h2>
                        <% if (note.pinned) { %>
                            <span>📌</span>
                            <% } %>

                    </div>
                    <p class="mb-2 text-gray-600 whitespace-pre-line">
                        <%= note.content %>
                    </p>
                    <div class="flex justify-between text-sm ">
                        <span>Created at: <%= new Date(note.created_at).toLocaleString() %></span>
                        <div><a>edit</a>
                            <form action="" method="post" class="inline">
                                <button>delete</button>
                            </form>
                        </div>
                    </div>
                </div>

                <% }) %>

        </ul>


        <%- include('../includes/footer.ejs') %>